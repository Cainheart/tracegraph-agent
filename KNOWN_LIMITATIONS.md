# Known Limitations

TraceGraph Agent 当前是 P0 `v0.1-alpha`，目标是证明可检查的 Agent Harness 纵向链，不是生产级自治 Coding Agent。

## 未完成项映射

下表只收录仍需后续能力补强的显式 gap；完整结构化映射位于
[`docs/known-limitations-map.json`](docs/known-limitations-map.json)。`mapped` 表示当前**限制边界**已有代码、测试和可执行验证，并不表示缺失能力已完成；尚无实现/测试接缝的条目必须诚实标为 `unmapped`。

| ID | 未完成能力 | 当前映射 | 后续条目 |
|---|---|---|---|
| `LIM-SANDBOX-PORTABILITY` | Linux/Windows 受限 native sandbox backend | `mapped` | G-13 后续 |
| `LIM-PROVIDER-NETWORK` | Host provider 网络白名单/代理 | `unmapped` | 后续安全项 |
| `LIM-MULTIHOST-COORDINATION` | 跨 Host 共识、锁与幂等协调 | `unmapped` | G-08 后续 |
| `LIM-TELEMETRY-STACK` | 完整 OTel SDK、持久队列与真实 Collector | `mapped` | G-15 后续 |
| `LIM-CREDENTIAL-PORTABILITY` | Windows/Linux 原生系统密钥库 | `mapped` | G-19 后续 |
| `LIM-GENERAL-ACTION-RECOVERY` | Patch 之外的通用 Tool/外部副作用恢复 | `mapped` | G-04 后续 |
| `LIM-TOKENIZER` | canonical wire tokenizer/provider count adapter | `unmapped` | G-03 后续 |
| `LIM-RETRIEVAL-ADVANCED` | 向量/embedding/Qdrant、Web Memory 管理与自动全仓摄入 | `mapped` | G-21 后续 |
| `LIM-SEMANTIC-CODEGRAPH` | 动态调用、语义诊断与完整 UI 投影 | `unmapped` | G-20 |
| `LIM-LSP-PLATFORM` | 原生 stdio LSP 已支持诊断/definition/references；完整 workspace indexing、自动 Context 注入、HTTP/SSE、跨 Host session 与 G-20 `code_intel` 合并仍缺失 | `mapped` | G-12/G-20 后续 |
| `LIM-SUBAGENTS` | 非阻塞父循环、任意多轮 child 续聊与 worker 自动恢复/重派 | `mapped` | G-07/G-08 后续（异步/分布式边界） |
| `LIM-MCP` | MCP 仅开放 Host-owned stdio/native bridge；PTC、HTTP/SSE、resources/instructions、远程 registry 与官方 filesystem e2e 仍未开放 | `mapped` | G-11 后续 |
| `LIM-EXTENSION-LOADING` | 任意第三方模块加载、隔离与签名/供应链 | `mapped` | G-17 后续 |
| `LIM-SKILL-DISTRIBUTION` | Skill 仅支持本地 `SKILL.md`；无网络/npm 安装、签名分发、脚本执行或跨 Host registry | `mapped` | G-10 后续 |
| `LIM-REAL-EVALS` | 真实 provider/Collector/生产网络评测 | `mapped` | G-16 后续 |
| `LIM-PRODUCTION-HOST` | TLS、生产鉴权、多人权限与部署封装 | `unmapped` | 后续平台项 |
| `LIM-SSE-RESUME` | 刷新/离线/令牌更新后的 durable cursor | `unmapped` | G-23 后续 |
| `LIM-BROWSER-E2E` | Playwright 真实浏览器自动化 | `unmapped` | 后续 G-22 增量 |
| `LIM-WEB-SCALE-UX` | tail-follow、虚拟化与中尺寸 Files Drawer | `unmapped` | Web 后续 |
| `LIM-PROVIDER-STREAMING` | 安全 `public_delta` token streaming | `unmapped` | 后续模型项 |

G-22 已补齐 CI、覆盖率、锁文件/供应链审计与私有构建产物验证；GitHub-hosted workflow 仍需在代码进入远端仓库后取得实际 run URL，不能用本地命令伪造远端执行证明。

## Runtime 与安全

- 受管项目和经原生目录选择器明确授权为读写的本地目录支持受策略约束的写入；以只读方式打开的目录始终不能被 preset、规则或客户端提权。默认 `workspace-write` 需要 Preview 与一次性人工审批；显式 `full-write` 不询问，但仍不能制造 WorkspaceHandle 没有的能力或绕过 schema、路径、hash 与 WAL。
- G-06 把 `read-only / workspace-write / full-write` 三种命名预设分别固定为 `read-only + never`、`workspace-write + on-write`、`danger-full-access + never`。CLI flag / environment 建立 Host ceiling；Web 只能从 `available_presets` 选择等于或低于 ceiling 的 key，并写入私有 `~/.tracegraph/harness-config.json`。项目 `<root>/.tracegraph/policy.json` 是独立的 `ask/deny` 收紧层，不能增加 allow。设置变更只影响之后创建的 Run；每个活动 Run 与恢复 Run 使用启动时冻结的分层 effective policy 与 digest。
- G-13 的 OS boundary **只包住 `run_test` 子进程**，不是整个 Host、容器或通用命令执行服务。实际 sandbox mode 来自该 Run 的 G-06 preset；浏览器的 StartRun/StartChat 请求没有 preset、sandbox、approval policy、rule 或 token 字段，不能逐 Run 扩大策略。`read-only` 会在 Runtime hard-constraint 层于 mutation 前拒绝 `side_effect:"write"`，但不会改写 WorkspaceHandle。
- macOS 受限模式会先校验 canonical workspace、可信 `/usr/bin/sandbox-exec` 并执行 capability probe；成功后才报告 `enforcement:"full"`。Seatbelt profile 允许读取 workspace 与进程所需系统路径，`read-only` 禁止所有写入，`workspace-write` 只允许 workspace 内写入，两者均 `deny network*`。若后端缺失、探测失败或执行报告不可信，则 durable 报告为 `none` 并列出 `unmet_constraints`，`run_test` 不启动。契约与 Web 能表达 `partial`，但当前 native runner 只产出 `full` 或 `none`。
- Linux 当前会探测 `/usr/bin/bwrap` / `/bin/bwrap`，但 **bwrap runner 尚未启用**；即使二进制存在也报告 `linux_sandbox_backend_not_enabled`。Windows 也没有 sandbox backend。两平台在受限模式下都会诚实返回 `enforcement:"none"` + 文件/网络未满足项，并拒绝启动 `run_test`，不会静默降级成直跑。`danger-full-access` 是唯一显式直跑路径，报告 `none` 且留下 `sandbox.disabled`；它仍保留 canonical cwd、无 shell、环境白名单、timeout、输出上限与进程终止边界，但没有 OS 文件/网络隔离。
- 模型 provider 请求由 Host 自己执行，位于 child sandbox 之外；当前没有 Host 网络白名单代理，所以 Seatbelt 禁网只证明测试子进程不能联网，不限制模型 `base_url`。`commit_patch` 也仍是 Host 的受控文件 API，不经 SandboxRunner：`read-only` 先拒绝，`workspace-write` 默认请求人工批准，未被规则收紧的 `full-write` policy allow 则不产生 approval request/grant/token。三者都继续依赖 Workspace capability、canonical action/policy digest、原子替换、Action WAL/对账与 cancellation shield；只有最终 `ask` 分支额外依赖 one-shot token。G-11 MCP server 仍在 Host child sandbox 之外，当前只接入 stdio/native bridge；不要把它解读成 provider、MCP PTC/HTTP/SSE 或所有 Host 文件 I/O 已隔离。
- G-06 的 `policy.evaluated`/`policy.denied` 是决策审计，不是路径安全的唯一防线。Runtime 会在记录 `tool.started` 前最终重评，deny 不会进入 WAL/mutation；但 `path_glob` 只匹配规范化的 workspace-relative 逻辑路径，真实路径、symlink containment、敏感文件和 Patch target 仍必须在 Workspace/Tool 边界再次校验。策略检查不能消除其它进程在 check 与 use 之间改盘的 TOCTOU，Patch 仍靠 canonical target、base/patch hash、原子替换与 G-04 WAL 收口。
- G-09 Plan Mode 只允许 Runtime 可信白名单中的只读工具，以及 `side_effect:"none"` 且只写当前 Run Ledger 的 `todo_write`。未来新注册的 `read/none` 工具不会自动获得 Plan 权限；非白名单调用在 Tool/WAL/工作区 mutation 前写 `policy.denied{reason:"plan_mode"}` 并失败。Plan 审批只是把同一 Run 转为 execute，不代替之后每个动作的 G-06 策略与一次性 Tool 审批。
- Todo 不是另一份数据库；`todo.created/updated/completed/blocked` 是 canonical Ledger 事实，读模型每次由事件重放。创建时依赖必须已存在，任何成环更新都会返回确定的闭环路径并拒绝。模型把 Todo 置为 done 必须引用同 Run、先于本次 mutation 的 eligible 独立成功执行事实：成功的非 Todo `tool.completed`、带严格成功 `run_test` Receipt 的 `test.completed`，或 allowlist 中的 `action.verified/action.reconciled`、Patch/Graph Event。Todo/Plan/Model/Policy/Approval/Run lifecycle 与 `todo_read/todo_write` completion 都不能自证；已有证据的 done Todo 在保持 done 时也不能清空它。证据只证明 durable execution fact，不检查它是否在语义上足以完成该 Todo。用户勾选不需伪造工具证据，actor-bound `todo.completed{updated_by:"user"}` 本身就是对该 Todo 的人工确认，但不能被模型拿去证明另一个 Todo。重开已完成 Todo 会保留历史，但若会使已完成的下游依赖失效则 fail-closed。模型侧 `todo_read` 是 item 分页接口（默认 25、最多 100），必须沿 `next_offset` 读取后续页；专属 content/result envelope 为 512/640 KiB，模型 excerpt 仍最多 4,000 UTF-8 bytes，完整页进入 current-Run Artifact。它不是一次返回全 Run 的无限大数组。
- G-07 已接通有界父子 Run 委派：模型仅能选 Host-owned trusted profile name、task packet、`isolated|fork` context scope 和请求预算，不能提交 provider、role prompt 或 tool allowlist authority。解析后的 provider key、role prompt/version/hash、tool allowlist、depth 与有效预算会作为冻结 `SubagentSpec` 同时绑定父子；child 使用独立 Run/Session/Ledger，父账本只保留有界 `subagent.started/message_sent/terminal` 回执。默认 `max_depth=1` / `max_parallel_subagents=2`，tool allowlist、budget 与 parent 终态门均 fail-closed。
- G-08 已在 root/coordinator Ledger 上实现 durable roster、共享 task board、member/coordinator mailbox 与显式 heartbeat/sweep；`coordinator` 是 mailbox 保留地址，不能作为成员 `subagent_id`。mailbox deliver/claim 可重放，task optimistic version 保证单 Host 同一任务只有一个 owner，`team.member_lost` 与该成员 claimed task reopen 是同一事实且不会自动重派。late create+running-child backfill、后续 started+join 与 sweep 的全部 loss+`team.sweep_completed`（含空结果）各自以同一 root JSONL 的一次 durable replace 提交；旧 partial sweep tail 仍可用原 command id 补齐。complete 的生产写入只接受当前 owner 对应 canonical child Ledger 中的 eligible durable evidence，伪造/其他 child id 及五个 Team 控制 Tool receipt 会被拒绝；root `team.task_completed` 提交后作为已验证 receipt 重放，不再跨文件复验。`appendAtomic()` 仅是单份 root Run Ledger 的原子替换，不是 root/child Ledger、Session、Workspace、外部系统或跨 Host 事务。`spawn_subagent` 仍在父工具调用内同步等待 child 终态；Team worker 并行只来自 G-05 同轮 batch + G-07 默认 permit。当前没有跨 Host consensus、旧 worker 自动重启、自动任务重派、任意模型轮次续聊或通用非阻塞父循环。
- G-17 的 `ExtensionManager` 是受信进程内组装层，不是恶意代码沙箱。标准 CLI 只读 strict、最大 256 KiB 的数据 JSON，并把 `module` 解释为 Host-owned trusted catalog key；当前精确只有 Artifact Tool 与 Run-state Tool 两个内置 id。它不会执行项目的 `tracegraph.config.ts`、本地 JS/TS 路径或 npm specifier，也没有签名插件分发、独立进程/容器隔离、自定义 Web UI 注入。
- G-12 的 `LspManager` 是 Host-owned native stdio seam，不是完整 IDE language platform。它按项目懒启动配置中的 language server，使用 `shell:false`、Content-Length framing、超时/Abort、Workspace containment 与 bounded diagnostics summary；`get_diagnostics` 的完整列表只停留在当前 Tool observation，Ledger/Projection 只保存摘要/sample/hash。未安装或启动失败的 server 返回 `unavailable` 并追加 canonical `lsp.server_unavailable`，不会盲目重试或伪造空诊断。LSP child 仍不自动获得 G-13 OS sandbox、G-04 WAL 或外部副作用恢复能力；自动 Context 注入、完整索引、HTTP/SSE、跨 Host session 与 G-20 `code_intel` 合并留后续。
- 扩展热重载是 idle-only：任一 active Run lease 存在时 reload/deactivate fail-closed，不对活动 Run 做 HMR。Run 在私有 recovery v5 Artifact 中冻结 generation/config digest/extension 和 Tool 集，恢复不一致会拒绝继续。Event hook/Context strategy 失败会隔离为唯一 `extension.error`，该 Event 不再分发 hook；Tool 业务失败仍以 Receipt/`tool.failed|unknown` 表达。扩展策略不能绕过 preset ceiling、Plan Mode、Workspace capability、Sandbox 或 WAL。
- G-14 的 steering 是每 Run、durable、最多 100 条的 mailbox，不是多 Agent/跨 Host inbox。`message` 与 `approve_hint` 正文最多 8,000 字符，每个安全点只消费一条普通 FIFO 队头；安全点定义为当前工具批次已经收口、下一次模型请求尚未发出。Runtime 先确认仍有下一 model turn，预算耗尽时输入保持 pending。`approve_hint` 仅作为下一次 history 的 user message，不会批准 Plan/Patch、绕过 G-06 policy 或生成 approval token。队列、终态选择与输入消费通过当前进程的 per-run control queue 原子协调，但没有跨进程共识；两个 Host 不应同时写同一 Run。
- `cancel` 先写 `user.input_queued`，再中断当前模型；它是 FIFO 的 control-lane 例外，可优先于更早的普通 pending message，因此取消后的投影可能保留未投递输入。工具已开始时必须等 Runtime 到批次边界，`commit_patch` 进入不可逆阶段后仍先完成 WAL、Receipt、Patch/verification 事实，最后消费 cancel 并写唯一 `run.cancelled { reason:"user_cancel", last_sequence }`。普通 `interrupted` Run 不自动续跑；若 prior running/indexing/待批状态已经 durable 排入 cancel，显式 resume 只做最小 consume+cancel 收口，不重发旧工作。取消不会自动回滚已提交 Patch。忽略 AbortSignal 的第三方 executor 仍可能在 Runtime 终止等待后占用后台资源，这一限制与 G-05 相同。
- G-14 的 durable digest/alias 负责跨重启幂等，但 `#commandRuns` / `#inputCommandRuns` 这两个进程内快速去重 Map 当前不回收，长寿命 Host 的内存会随命令数增长。Ledger append 与 Session `event_ref` 也仍是顺序双写；steering retry 能针对本命令补索引和重武装取消，但不是所有历史 Event 的通用反向索引事务。
- G-15 Telemetry 是从**已经成功 append 的 canonical Session Event** 白名单派生出的 best-effort 旁路。sink 的 `emit()` / `flush()` 抛错会被吞掉并累计 `error_count`，不能改变已提交 Run 的成功/失败结果；反过来，Telemetry 丢失、重复或不可用也不能用来补写 Ledger。Ledger 仍是唯一事实源，恢复、Projection、Action 对账和审计都不读取 Telemetry；启动也不会把历史 Ledger 回填给 sink。
- Telemetry 默认 `noop`，缺少 `<dataDir>/telemetry.json` 时即使存在 `OTEL_EXPORTER_OTLP_ENDPOINT` 也不会联网。只有该服务端文件显式选择 `otlp_http` 后才会读取 `endpoint_env` 指向的环境变量；可选 `authorization_ref` 必须是 `${secret:NAME}` 并经 G-19 CredentialStore 解析。当前没有从浏览器修改配置的路径，也不接受明文 authorization 字段。旧 Host/SDK 没有 status 能力时，Web 会显式呈现不可用/查询错误，不会把未知状态伪装为 `noop / disabled`。
- OTLP-HTTP 当前是小型 vendor-neutral sink，不是完整 OpenTelemetry SDK：没有自动 retry/backoff、磁盘持久队列、processor/exporter 生态、sampling、propagation 或 collector discovery。只有网络/超时失败与 HTTP `429/502/503/504` 会把对应 signal 批次重排到有界内存队列；`400` 等不可重试响应及 HTTP 200 `partialSuccess` 拒收会计错并丢弃该批。非 200 不读正文；HTTP 200 正文最多读 64 KiB 以检查 rejected count，Collector 正文/错误消息不会进入 status 或错误文案。队列、`error_count` 与 `last_error_at` 都只驻留当前进程，重启即丢失；被保留的可重试批次在之后 flush 时可能形成重复投递。CLI 关停在 Host close 后只给 Telemetry flush 5 秒总等待预算，超时不保证队列已排空。现有自动化使用 mock fetch 与统一 conformance suite；后者通过 `deliverySnapshot` 比较两次 flush 的已交付结果，但尚未对真实外部 Collector 做集成验证。
- Approval token 只存在于最终 policy 为 `ask` 的路径，是 Host 内存中的 opaque one-shot capability，不是签名 JWT、远程 RBAC、跨进程凭据或 workspace lock。它精确绑定 approval/project/run/action、action digest、policy digest、scope 与 expiry；失配先 burn 再拒绝，同一 token 只有一个消费者能成功。进程重启不会恢复 token，待审批恢复会在验证冻结策略与 recovery Artifact 后重新签发。policy `allow` 不签发 token，也不伪造 approval request/grant。Patch 在批准时重算 action/canonical-target binding，并在 mutation boundary、WAL 前再次核对 canonical target digest，但这仍不是跨进程文件锁。非 Patch `ask` 依赖 Host 注入的自动 answerer；answerer 缺失、抛错、返回非法结果或报告 unavailable 都会 fail-closed。当前 CLI composition 没有注入该 answerer；Web/SDK 手工 approve/reject 只处理 Patch ask，不能代答普通 Tool ask。
- macOS 上持久化模型凭据使用系统 Keychain；非 macOS 当前没有 DPAPI/libsecret 等系统密钥库集成，回退到 `~/.tracegraph/credentials.json`。在 POSIX 上父目录收紧为 `0700`、文件强制 `0600`，并拒绝 symlink、非普通文件、错误 owner/权限与超限载荷，但凭据仍是 plaintext-at-rest；Windows 的 `chmod` 不等同于完整 ACL 保证。Keychain `list()` 只报告当前进程已读取/写入的安全元数据，不做整个钥匙串的枚举。环境变量是只读来源，不能从设置页或直接配置 API 覆盖。
- `model-config.json` 只持久化 `${secret:NAME}` 引用，旧版明文 Key 会在启动时迁移；选定持久后端若读取失败会 fail-closed，不会静默改走另一份明文。模型请求仍必须在本机 Host 内短暂解析出明文并放入供应商请求头，当前没有内存锁页、硬件绑定或主动内存清零保证。
- 已接入 OpenAI-compatible Chat Completions 与 Anthropic Messages；各提供商的模型名、推理强度和账户权限仍由提供商决定，配置预设不等同于连通性或可用性保证。
- UI 可请求 `default / low / medium / high / xhigh / max`。Adapter 只向已知支持的模型发送对应参数，必要时映射到较低档位；未知或不支持的组合回退为提供商默认值。
- Host 启动会扫描 durable Session：先修复受支持的 Session 尾部，再对当前仍已注册、可解析出可信 `WorkspaceHandle` 的 Run 做 Patch Action WAL 对账，最后才收口其它无终态 Run。当前 v5 recovery state 在 G-06 的 Host/project rule layers、policy digest 与 G-09 `plan|execute` 模式上，固化 G-07 orchestration depth/limits/delegation，并冻结 G-17 extension generation/config digest/extension 和 Tool 集；v1/v2/v3/v4 继续只读兼容，旧 `manual` 仅在 v1/v2 边界映射为 execute。待审批 Patch 只按该冻结策略重新签发一次性审批；待审批 Plan 恢复到原 `plan_event_id`/Todo 版本，两者都不采用当前用户设置也不自动执行。扩展 snapshot 不一致也不会用新工具面继续旧 Run。G-07 恢复会对账父 active link：child 已终态时补写绑定其 terminal event id/hash 的父回执；child 未终态时先递归收口 descendants、写 child `run.cancelled`，再补父 interrupted/terminal 回执，保证 parent 不早于 child 终态。这个流程不会自动重启旧 child 模型循环。若 child terminal 已 durable、但父 terminal receipt 持续写不进，当前进程选择 fail-stop：父保持 `running`、permit 不释放，必须重启 Host 才会进入上述 reconciliation；它不会在同进程释放 permit 或重启 child。除 G-04 的 `commit_patch` 对账外，其它 Tool/外部系统副作用没有通用自动恢复能力。
- G-04 只覆盖单目标 `commit_patch`。WAL 在改盘前保存 before/after hash、canonical workspace root digest 与既存文件的精确 before-image；恢复遇到 after hash 会补齐 durable Patch/Receipt/verification 事实，遇到 before hash 会关闭为 `aborted`，其它状态写 `action.diverged` / `needs_manual_review` 并保留用户文件。它不覆盖 provider usage、`run_test` 或未来任意外部 Tool 副作用，也不对已取消注册/无法解析工作区的 Session 自动触盘。
- Action rollback 已有 Host API / SDK，但 Runtime 与 CLI **默认关闭**。`TRACEGRAPH_ROLLBACK_ENABLED=true` 只允许 disposable workspace 进入回滚；managed/读写 linked workspace 还要求 `TRACEGRAPH_ROLLBACK_ALLOW_FORCE=true` 且请求 `force:true`。所有回滚都要求 quiescent Run、verified WAL、同一 canonical workspace、单目标且当前内容完整匹配 after hash；`force` 不会绕过这些条件。第三方修改会写 `action.rollback_refused`，不会被覆盖。当前 Web 只展示相关事件和人工复核状态，没有回滚按钮。
- WAL、Recovery Ledger 与 exact before-image 以本地私有目录/文件权限和 symlink/owner/hash 检查保护，但不是静态加密。写队列只在单进程内串行；Session lease 也不是跨进程 workspace target lock。两个 Host 或其它进程并发修改同一文件时仍可能竞争，因此不得把 G-04 解释成通用文件事务或多进程锁服务。
- G-05 的批调度先预校验整批，再只并行 `concurrency_safe && side_effect !== "write"` 的连续安全波，默认峰值 4；写操作、需审批或并发不安全的调用串行，结果/Observation 仍按请求顺序落账。批调度与 per-run Event/Session append 队列只在当前进程内协调；Artifact 是逐工件原子 durable 写入，也没有跨进程协调。它们不提供跨 Host 锁；G-13 只为内置 `run_test` 增加上面所述的子进程沙箱。
- 通用 Tool timeout 通过 `AbortSignal` 与 `Promise.race` 保证 Runtime 在期限内停止等待，但取消是协作式的：第三方 executor 若忽略 signal，其资源仍可能在后台继续。`commit_patch` 在最终取消检查后、原子 rename 前会进入 Host-only cancellation shield；一旦进入不可逆写阶段，stop/timeout 必须等待 applied WAL、Receipt 与 Patch Event 收口，不能先返回一个假的 `tool_aborted`。内置 `run_test` 是另一种更强的特例：它经 SandboxRunner 启动；POSIX 下运行在独立进程组，按 SIGTERM→SIGKILL 终止整组且限制输出。Windows 受限 sandbox 模式当前不会启动 child；若显式使用 `danger-full-access`，仍只有直接 child 终止，P0 没有跨平台 process-tree kill 保证。
- `list_artifacts` 只列出当前 project/Run 的公开 Artifact 元数据并分页，不返回正文，也排除内部 `recovery_state`；Runtime 不会把这份清单再次保存成 `tool_output` Artifact，避免自我递归。精确内容仍须走受 kind/scope/hash/大小约束的读取路径。

## Context、Memory 与 Graph

- ContextBuilder 已提供可审计的默认 Context Policy：总窗口 `258_000`、预留输出 `32_000`、输入预算 `226_000`、70% 预警和 80% 压缩阈值。这个 `258K` 是 Harness 策略，不是已向每个 provider/model 协商或验证过的最大窗口；当前没有 provider capability adapter 自动把策略收敛到模型的真实窗口。
- preflight token 预算默认从保守的 `heuristic_v2` 起步，并会用已完成请求的 provider-reported input usage 按 `(provider, model)` 滑动窗口校准。`TokenCounter` 是可注入接缝，但仓库没有内置 tokenizer，也没有 Anthropic `count_tokens` 调用适配器；未知模型或 counter 失败会安全退回启发式估算。
- `ContextManifest.token_estimate.per_section` 与每个 item 的 token 数都是**请求前对可见 section 文本的分配**，不是 provider 回报的逐 section 精确账。即使外部 counter 能精确分词片段，它计算的也只是 meter 收到的 section 文本，不含 provider system prompt、JSON envelope、`[section]` 前缀/分隔符或重复封装的 task/observations；所以当前 preflight confidence 仍为 `estimated`，收到历史 provider usage 后才为 `calibrated`，契约里的 `exact` 保留给未来覆盖 canonical full-wire request 的 counter。调用结束后的 `model.usage_reported.total_tokens` 才是提供商回报口径；服务端仍可能以 `context_length_exceeded` 拒绝请求。
- 默认 Runtime 把校准文件写到 `<dataDir>/token-calibration.json`，以 provider/model 隔离的有界滑动窗口更新。目录/文件在 POSIX 上收紧为 `0700`/`0600`，拒绝 symlink、错误 owner、过宽权限、超 1 MiB 或损坏载荷；但串行队列只覆盖单进程，没有跨进程 writer lock。两个 Host 共用同一 `dataDir` 时，校准文件可能 last-writer-wins，当前不保证合并全部样本。
- OpenAI-compatible 与 Anthropic 的 JSON/SSE 路径会解析完整响应中的 usage，并把初始请求写成 durable `model.usage_reported`；截断的 SSE 不会被误当作完整 usage。相对 preflight 输入偏差超过 ±25% 时再写 `model.usage_anomaly`。OpenAI 格式修复请求作为独立 `request_kind: "repair"` 记录，但因其请求形状不等同于 Context Manifest，不参与校准，也没有 `delta_ratio`。成本只有在 provider 明确同时返回合法金额与三字母币种时才标为 `provider_reported`；否则明确为 `unavailable`，系统不按价目表估算费用。Web Context 卡片显示同一 model call 中 sequence 最大的单次请求（可能是 repair），不是把 initial+repair 合成会话总账；两条原始 Event 都保留在 timeline。
- usage 是 best-effort accounting metadata。若进程在 provider 已返回、但 `model.usage_reported` 尚未 append 之间崩溃，Ledger 可能缺少这次用量；校准样本当前先安全落盘再 append usage Event，因此极窄窗口内也可能出现“派生校准已前进、Ledger 尚无对应 report”。当前没有 usage WAL、从 Ledger 重建校准或 provider 侧对账；已经实现的 G-04 是 Patch Action 专用，不覆盖这一窗口。新增 `model.usage_reported` / `model.usage_anomaly` 仍沿用 `tracegraph.session-event.v1`；把 v1 事件枚举硬编码为旧集合的 strict 消费者必须升级后才能读取这些账本。
- G-02 已把 Context 升级为有序策略链：`tool_output_pruner → spill → model_summary → tiered_checkpoint`。该压缩链只处理 conversation history 和 tool Observation；G-21 会在压缩前另行召回并注入 `memory/retrieved`，当前不对 retrieved 节点做模型摘要或 spill。G-18 附件使用独立的 staging/claim 与 offload/inline 策略，不混入这条文本压缩链。各步必须严格减少 token，按固定策略顺序形成连续账，并完整解释 `before_tokens - after_tokens`；`SpillRef.locator` 也必须严格绑定同一 `artifact_id`。
- G-18 只支持把 PNG/JPEG/PDF 附加到下一项新任务，单个最多 5 MiB、每 Run 最多 8 个；运行中 steering 不能追加附件。图片默认 offload，只有用户显式选择且 Host 配置 `image_input` 时才 inline，标准装配不会从 provider/model 名称猜测能力。PDF 内置抽取器只做保守 literal-text 提取，不做 OCR；失败时原 PDF 仅保留 reference。Replay 只重放附件 metadata/status，不开放二进制预览或再次向模型发送图片。Host raw body 6 MiB 是 transport hard cap，不是产品额度。
- `model_summary` 只接受 `{facts[], open_questions[], refs[{path,lines}]}` 严格结构，并保留独立 `model_call_id`、`prompt_version` 与 `request_kind: "summary"` usage。摘要是不可信的压缩表示，不是事实真值；需用 `refs` 和已归档原文回查。provider 未配置/不可用、超时、请求失败、输出非法或不足以缩减时，Runtime 写 `context.summary_failed` 并降级到确定性 checkpoint，而不把伪造结构当成摘要。
- spill/摘要/checkpoint 替代的原文保存在 `<dataDir>/artifacts` 的 `spilled_tool_output` / `context_source_archive` 中，summary/checkpoint 的可见表示保留 `artifact:<id>` locator。只读 `read_artifact` 严格绑定当前 Run（调用方不能自选 project/run），从 `offset:0` 起按 `next_offset` 分页，每页最多 4,000 UTF-8 字节，并重验 kind/scope/content hash；它不依赖 Workspace 文件能力，所以 Plain Chat 也能回读自己这一 Run 的 spill，但不能读取本机项目或其它 Run。Artifact Store 有 `0700`/`0600`、symlink/owner/hash 检查，但不是静态加密。Web 只能展开公开 Wire 上限内的原文；Runtime internal 单工件仍受独立 8 MiB 上限限制，不是通用大文件管道。
- 路线图的“300k tokens”验收是合成长历史在当前 preflight 计量口径下超过 258K，再验证策略链将请求收敛到 226K 输入预算内。它不证明所有 provider 都接受 258K/300K wire request，也不是摘要事实正确率或长期记忆质量评测。
- G-21 已实现 strict Memory candidate 评估、canonical JSONL 持久化、project-isolated BM25 检索（`global` Memory 进入专用 global index）、Runtime 自动召回与 `memory/retrieved` Context 注入，并在 Ledger 保留 query hash、命中 id/score/token 及 path/line provenance。默认生产组装仍是本地 JSONL + lexical BM25；独立 retrieval service 只在显式配置时使用，不是默认依赖。
- 尚未实现 embedding、向量检索、reranker 或 Qdrant 后端；可注入的 embedding provider 只是 seam，不是已落地能力。当前也没有 Web Memory 管理/删除/人工审核 UI，不会自动爬取整个仓库或监听文件变化；只有显式 ingest 的来源和被 `remember()` 接受的 Memory 可检索。Memory/Index 写入仅在单进程内串行，不是跨 Host writer consensus。
- CodeGraph 只表示 TS/JS file/module/static import/export；动态 import、运行时调用、DI、路由和跨服务关系可能是 unknown/partial。
- CodeGraph 的扫描、读取和 Compiler 调用前后均检查取消与 deadline，但 TypeScript 的同步 `updateSnapshot()` 无法在调用内部硬抢占；信号会在它返回后生效。
- Core Provider 当前只消费已校验的 Graph Snapshot/Delta；recoverable partial 的完整 coverage 以及诊断 `code/severity/line` 尚未进入 Projection 与 UI。
- G-07/G-08 提供上述有界父子 Run 与 root-Ledger Team 协调，但没有跨 Host Team 调度、自动 worker 恢复、MCP 的 PTC/HTTP/SSE/resources/instructions 或 Langfuse；G-15 只有上述最小 OTLP-HTTP 导出，不等同于完整 OTel SDK/平台集成。

## 评测与发布

- G-16 已提供与 `pnpm test` 分离的离线 `pnpm evals`：首批 5 条 Runtime 行为回归已落地，G-07/G-18 后续各增 1 条，G-17 再增 1 个含 3 场景的评测文件，G-08 再增加 Agent Team 行为评测，当前共 9 条独立 Runtime 用户路径；另有 3 条质量对比、文档一致性审计与四项性能门。这些评测删除常见 provider 凭据并拦截非 loopback 网络；它们证明固定场景可重放，不证明真实 provider、外部 Collector 或生产网络环境的效果。
- G-23 的 `evals/replay/time-travel.eval.ts` 会先记录每个 sequence 的 canonical replay snapshot，再用新 Runtime 从同一磁盘 Ledger 逐点重建，核对 projection/hash/anchor、结构化 forward diff 与回放不追加 Event；backward/same diff 由 Core 单测覆盖。它证明 deterministic Ledger replay，不是 Playwright 真实浏览器交互证明，也不覆盖真实 provider。
- 原 `memory-context-quality.eval.ts` 仍只是固定 record 的 fixture seam；G-21 新增的 `retrieval-rag-quality.eval.ts` 才运行生产本地 JSONL+BM25 检索链，比较有/无检索的 task success 与 citation accuracy。它仍只有三条确定性离线任务，不是真实 LLM、线上语料或召回率基准。CodeGraph 质量评测在固定 TypeScript 故障上测量诊断增益，不是 LLM 的通用成功率或线上质量评估。
- `context.built.input_tokens` 与 model-call 数是确定性 gate；Tool P95 和 loopback SSE 首字节是本机 wall-clock 回归门，预留了 scheduler/startup 余量，不是跨机器 SLA。基线只能通过显式 `pnpm evals:update` / `--update` 改写，但当前仍依赖人工 review 判断基线变化是提升还是退化。
- 文档一致性评测只对明确白名单中的标识符、版本、Event、Host 路由和 package script 做可执行核对，不是对全部自然语言声明的语义证明。`_tmp_evals/reports/latest.json` 是可覆盖的本地诊断产物，不是 Ledger 或发布证明。
- G-22 已定义并本地核对 `typecheck` / `test` / `evals` 三个 CI job：eval/performance/docs gate、全局/关键模块覆盖率、exact lockfile、24 小时冷却、high/critical audit 与 tag/version/CHANGELOG/构建出口发布校验均会 fail-closed。发布产物是隔离 staging 中带 SHA-256 清单的私有 workspace build artifact，不是 npm 发布、签名、provenance 或生产部署。当前工作树没有可引用的 GitHub-hosted run URL；必须等代码进入远端后取得，不能把本地同构命令冒充远端 CI 证明。

## Host、Artifact 与恢复

- P0 面向本机 loopback Host，产品主会话仍是单活动 Project/root Run，仅允许其在 G-07 depth/parallel 上限内启动独立 child Run；不提供生产鉴权、TLS、多人权限和部署封装。
- G-09 的 `GET/POST /api/runs/:runId/todos` 和 `POST /api/runs/:runId/plan/approve` 都先从 canonical Projection 解析 project/run scope；浏览器不能提交 `project_id`、`run_id` 或 Todo actor。审批必须带当前 `plan_event_id`；等待期任何 Todo 变更都在同一 Run 内追加新 `plan.ready` revision，因此旧页面/并发请求不能批准已变更的计划。幂等命令表与 per-run 队列仍只是单 Host 进程内边界，不是多 Host 共识。
- G-14 的 `POST /api/runs/:runId/input` 同样从 canonical Projection 绑定 project/run/actor/timestamp，浏览器只能提交 strict `command_id/input_id/kind/body`。同一 `input_id` 相同原始 payload 是幂等重放，不同 payload（包括脱敏后碰巧相同的正文）会冲突；内部 digest 不进入 Wire。未消费输入在 Ledger/Projection 中跨重启保留，但普通 `interrupted` Run 仍按 G-01 只读恢复，不会因为 pending input 自动续跑 Model/Tool；terminal、`interrupted` 与 `needs_manual_review` Run 不再接受新输入。这里不否定已在崩溃前 durable 排队的 cancel：它只能通过显式 resume 进入最小终结路径，不是向中断 Run 新增输入。
- `GET/POST /api/permission-config` 是本机有界配置面，不是完整 policy editor。响应刻意不含 rules、path scopes、本机配置路径或 token；请求只能提交 Host 广告的 preset key，且 command-id 幂等表只在当前 Host 进程内。CLI/environment ceiling 变更仍需重启 Host；用户级选择无需重启 Host，但只影响新 Run。
- `GET /api/telemetry-status` 只返回 strict `tracegraph.telemetry-status.v1` 状态：sink、`disabled|active|degraded`、非负错误计数与可选最后错误时间。它不返回 endpoint、header、credential、配置路径或 pending payload，也没有对应 POST/PUT 配置接口。该状态是进程内健康快照，不是 durable Run 状态；重启后计数与最后错误时间会重置。
- `PolicyEngine` 支持可编程 Host rules，但当前 CLI composition 没有单独的 Host rule 配置文件或编辑器：实际产品装配使用内置 preset 默认，再叠加项目 `.tracegraph/policy.json` 的 ask/deny 规则。若嵌入方直接注入 Host rules，仍必须在 Host 信任边界完成校验；浏览器设置面不能创建它们。
- Session JSONL 是可搜索索引而不是第二份事实账本：每个文件只保存版本化 header 与带 `entry_id` / `parent_entry_id` 的 Event 引用；Run 事实仍以 canonical Event Ledger 为准。G-07 child Session 在 header 中带 `parent_session_id`，普通历史查询默认 `view=roots` 不把 child 混成顶层对话；`view=all` 只用于受信检查，Web 的 child 访问另经父子 relation-scoped 只读路由。恢复上下文与精确待审批 `PatchInput` 只保存在内部、脱敏且校验哈希的 Artifact 中，不经公开 Wire Projection 返回。Artifact root 会收紧为 `0700`、data/metadata 文件以 `0600` 创建并拒绝 symlink root；这些是本地文件权限边界，不等同于静态加密。
- Ledger append 与随后追加 Session `event_ref` 是顺序双写，没有跨文件事务。若进程恰好在两步之间崩溃，Ledger 事实仍有效，但 Session 索引可能暂时缺少最后若干引用。G-04 恢复相关 Patch Action 时会用稳定 idempotency key 重新提交相同提案，从而补齐这些 Action Event 的缺失引用；当前仍没有扫描所有 Ledger Event 的通用反向补索引。
- Session `event_ref` 当前只有 event/run/sequence，没有 Event Ledger 的 data-root locator。因此一个 Session root 必须始终与创建它的同一个 `dataDir` 配对；不能让多个独立 Ledger 共用同一 `--session-dir`，也不能单独搬运 Session JSONL 后期待跨 dataDir 浏览。默认组合是仓库 `<repo>/.tracegraph` + `~/.tracegraph/sessions`，它隐含单一 TraceGraph data root 的使用方式；若使用多个 `--data-dir`，必须为每个实例同时指定独立、稳定的 `--session-dir`。配对错误会在启动扫描时 fail-closed。
- Session Store 采用单写者租约；追加、改名、软删除和尾部物理修复都要求有效租约。不完整的最后一行只在租约内截断，并追加 `session.tail_truncated`。同机死亡 PID 可立即接管，跨主机/无法确认存活性时依赖心跳超时。租约冲突会 fail-closed、由 Host 返回 409，并在 Host 结构化日志落脱敏的 `session.operation_rejected`；它刻意不绕过租约向目标 Session Ledger 写事件，因此 Host 日志的保存/轮转仍是部署侧责任。
- durable Event 的 SSE cursor 只保存在当前 Web 内存；刷新、长时间离线和 capability token 过期后的无缝恢复尚未实现。`LivePublicActivity` 另是一个刻意短生命周期的内存投影，用来让当前页面快速显示真实阶段；刷新后不保证逐行重现此前的闪现动画，规范可回放事实仍是 JSONL Ledger 与 Context Manifest。
- 公开 Artifact Wire 上限仍为 1 MiB，只允许 JSON、Markdown、plain text 和 diff，且没有 range retrieval。Runtime-only `getInternal()` 使用独立 8 MiB 单工件硬上限，并继续执行 scope、MIME、长度、hash 与二次脱敏校验；当前仅供有严格 schema/类型边界的 Session 恢复和 G-02 使用，不暴露为 Host 通用大文件路由。G-02 的 `read_artifact` 只是在该 internal 边界内提供当前 Run 专用、最多 4,000 字节/页的 Tool 回读，不改变公开 API。
- Projection Replay 只重建视图，不重新执行 Model 或 Tool。G-23 的坐标必须同时包含 `session_id + run_id + until_sequence`，因为 sequence 只在 Run 内有意义；snapshot hash 只承诺 canonical Ledger → `RunProjection`，不包含会继续增长的 head、token/expiry 或 Web 的连接/项目/volatile model/异步 Artifact 展示状态。G-01/G-06 的 Patch 恢复特例是“验证冻结 effective policy 后重新签发待审批 Patch 的审批”，真正 commit 仍必须由用户使用新 approval 明确触发；G-09 可执行恢复特例则只是重建等待中的 Plan/Todo 并继续等待精确 revision 审批，不自动进入 execute。legacy pending approval 若没有完整 action/policy binding 不能执行，其它未完成动作保持 `interrupted`。
- G-23 replay capability 是短时、只读且绑定 session/project/Run、冻结 head 与可见 Artifact 前缀的 Host authority；它只冻结当前回放会话的可见边界。进入回放会停止当前页面的 durable/live/model 订阅并禁用其写入口，但不会暂停后台 Agent，也不会撤销其它客户端仍持有的 live bearer。当前不支持从历史节点 fork、跨 Run 合并时间线或持久化浏览器 replay cursor；durable SSE cursor 跨刷新/长离线/令牌替换的恢复也仍未实现。

## Web 产品面

- Web 会从 durable Projection/Event 显示 Run 固定的 permission badge、策略 explanation/rule/source，以及 sandbox 徽标（`full` 绿色、`partial` 黄色、`none` 红色）。这些都是 Host 已记录事实的可视化，不是浏览器侧强制层。设置页只能选择 Host 广告且不高于 ceiling 的 preset，并显示 source/lock/full-write 警告；不能编辑 Host/project rules 或把活动 Run 热提升。紧凑视口沿用既有响应式规则隐藏顶栏 metrics，因此 header 徽标可能不可见，但相关 Trajectory 行仍保留证据。
- 设置页的 Telemetry 区只读显示 Host 返回的 sink/state/error count/last error；没有 destination、header、credential 或启停控件。浏览器既不能看到 secret，也不能把任意 endpoint 注入 Host；配置必须由操作者在服务端 `<dataDir>/telemetry.json` 与环境/G-19 凭据边界完成。
- Todo 面板展示 canonical Projection 里的单 Run 状态、依赖与证据引用，勾选/重开会通过 strict Host API 落账；它不是本地乐观数组的持久化。Plan 横幅只在 `awaiting_plan_approval` 时批准当前 revision，批准后仍是原 Run。G-08 另有 root-team scoped task board，但两者不是同一对象；当前仍不支持 Todo 拖拽排序、自由编辑依赖图或跨 Team/跨 Session 汇总看板。
- G-08 Team 面板只从 canonical `RunProjection.team` 展示 roster/task board/mailbox，并开放用户 steer 与任务 cancel；它不能伪造 worker heartbeat/claim/member identity。steer draft 只在 Host 明确确认成功、且请求期间用户未改写时清空；失败、抛错或响应不确定都保留 draft 供重试。Replay 中 Team 面板只读。显式 sweep 不等于后台 liveness daemon；未调用 heartbeat/sweep 时不会自行判断失联，重启也不会复活旧 worker 或自动接管其任务。
- Live UI 已有 Session 列表、标题搜索、软删除确认和恢复入口，但每个 root Session 当前只打开其最后一个 Run。G-07 只在父 Run 的 Subagent 面板中按需展开关系绑定的 canonical child Ledger，该视图是只读的；还没有独立全局 Run/child 搜索、按任意 `run_id` 导航、完整树分叉可视化或从历史节点 fork。
- Web 的 Context 面板从 durable Manifest/timeline 重建 preflight、provider usage 与压缩步骤，并可以显式展开通过公开 Artifact 路由验证且小于等于 1 MiB 的被压缩原文；超限、缺失或 hash 损坏时只显示不可用/损坏状态，不绕过 Host 边界。provider 总量不会被反推成逐 section 精确数字；Decision 请求和 summary 请求也保留独立 `model_call_id/request_kind`，不合并成一笔伪总账。
- 本地目录通过本机原生选择器注册并持久化；浏览器不能直接输入路径。登记表不是跨设备同步机制，目录被移动、删除或权限失效后需要重新选择。
- Plain Chat 使用隐藏的无能力工作区，不会索引、读取、搜索、执行或修改本机项目。唯一特殊只读入口是 G-02 `read_artifact`：它只能分页回读当前 Plain Chat Run 自己生成的 Context archive，不构成 Workspace 文件能力。需要理解工程时仍必须先选择项目。
- 选中轨迹事件会进入该 Run/sequence 的只读 Replay，并按该历史前缀渲染 Context、Diff、Graph Delta 和 Test Log；顶部 Replay 条支持按钮及 `←/→` 单步，“回到现在”会重新获取最新 Run 后再恢复 SSE。generation gate 会淘汰晚到的 live/replay HTTP、SSE 与 Artifact hydration，但并非所有 Artifact 都有专用视图。
- 没有真实浏览器自动化 E2E；现有证据为 Web 单测、Host/SDK E2E、Vite build 与 compact-view 人工 smoke。
- Trajectory 尚无真实 scroll-based tail-follow 与列表虚拟化。
- 1024–1279px 的 Files Drawer 交互未完成；小于 1024px 只显示只读宽屏提示。
- 对话中的“公开执行过程”通过 Live SSE 实时展示真实的 `context.*`、`model.request_started`、受控的结构化 `model.decision` 摘要和真实 read/search/tool 事件。它不直接转发模型自由文本的 `public_reason`，只传输脱敏摘要；不能展示、还原或合成提供商私有 CoT、`reasoning_content`、原始 Prompt、API Key 或原始工具输出；Live feed 不承担持久审计职责。
- 运行中 steering 已支持：running/indexing/待 Plan 或 Patch 审批时输入框保持可用，并显示 durable pending 数量；`user.input_consumed` 在 Trajectory 标出 `at_step`。只读权限预设不会禁止 steering，因为它不授予 Workspace 写能力；`interrupted`、`needs_manual_review` 与终态 Run 禁用。Stop 现在提交 durable cancel 并继续保持 SSE，直到 canonical `run.cancelled` 到达；legacy `/stop` 仍为兼容接口。
- 当前没有 provider token streaming 或安全 `public_delta` 文本流。模型输出首先是完整的结构化 `Decision` JSON，只有整体解析与校验成功后，公开回答才进入 Event Ledger 和对话视图；因此回答正文会一次性出现。Steering 改变的是下一次模型请求的 history，不会让当前回答变成 token 级流式输出。
- Approval 倒计时不是持续刷新；错误 UI 仍以基础提示为主。
- Markdown 中的 Mermaid 图由浏览器渲染为 SVG，模型对架构/流程/状态图请求会被提示使用语义化节点与分组；图的质量仍取决于生成的 Mermaid 源码。它不是图片生成模型，也不会把任意自然语言自动变成专业制图。Graph Before/After 只展示 Graph Delta 中真实存在的实体，缺失一侧会显示 unavailable，不等同于完整仓库快照、调用图或成熟的交互式架构分析器。
