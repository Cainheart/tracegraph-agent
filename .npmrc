# pnpm 11 reads non-registry project settings from pnpm-workspace.yaml.
# Keep this npm-compatible fallback so accidental `npm install <pkg>` writes
# an exact version instead of introducing a range.
save-exact=true
